/**
 * Public routes included in `/sitemap.xml` (TECH-11).
 * Add a path here whenever you create a new SEO-visible page.
 * Do not list auth-only or draft-only routes.
 */
export const PUBLIC_SITEMAP_PATHS = [
  "/",
  "/faq",
  "/imprint",
  "/privacy",
] as const;
